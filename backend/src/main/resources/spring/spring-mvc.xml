<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/aop
       http://www.springframework.org/schema/aop/spring-aop.xsd
       http://www.springframework.org/schema/mvc
       http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- ========== 扫描 Controller、AOP 切面和 Web 配置 ========== -->
    <context:component-scan base-package="com.aicode.controller"/>
    <context:component-scan base-package="com.aicode.aspect"/>
    <context:component-scan base-package="com.aicode.config"/>

    <!-- ========== 开启 AOP（在 DispatcherServlet 上下文中生效，才能拦截 Controller） ========== -->
    <aop:aspectj-autoproxy proxy-target-class="true"/>

    <!-- ========== 开启 MVC 注解驱动（自定义 Jackson 日期格式） ========== -->
    <mvc:annotation-driven>
        <mvc:message-converters>
            <bean class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter">
                <property name="supportedMediaTypes">
                    <list>
                        <value>application/json;charset=UTF-8</value>
                    </list>
                </property>
                <property name="objectMapper">
                    <bean class="com.fasterxml.jackson.databind.ObjectMapper">
                        <property name="dateFormat">
                            <bean class="java.text.SimpleDateFormat">
                                <constructor-arg type="java.lang.String" value="yyyy-MM-dd HH:mm:ss"/>
                            </bean>
                        </property>
                    </bean>
                </property>
            </bean>
        </mvc:message-converters>
    </mvc:annotation-driven>

    <!-- ========== 静态资源处理 ========== -->
    <mvc:default-servlet-handler/>
    <mvc:resources mapping="/uploads/**" location="file:./uploads/"/>

    <!-- ========== 文件上传解析器 ========== -->
    <bean id="multipartResolver"
          class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="10485760"/>
        <property name="defaultEncoding" value="UTF-8"/>
    </bean>

    <!-- ========== 拦截器配置 ========== -->
    <mvc:interceptors>
        <!-- 认证拦截器（排除公共接口） -->
        <mvc:interceptor>
            <mvc:mapping path="/api/**"/>
            <mvc:exclude-mapping path="/api/health"/>
            <mvc:exclude-mapping path="/api/captcha"/>
            <mvc:exclude-mapping path="/api/email/**"/>
            <mvc:exclude-mapping path="/api/user/register"/>
            <mvc:exclude-mapping path="/api/user/login"/>
            <mvc:exclude-mapping path="/api/user/reset-password"/>
            <mvc:exclude-mapping path="/api/stats"/>
            <mvc:exclude-mapping path="/api/tags/**"/>
            <mvc:exclude-mapping path="/api/rankings/**"/>
            <mvc:exclude-mapping path="/api/rankings"/>
            <mvc:exclude-mapping path="/api/learning-paths/**"/>
            <bean class="com.aicode.interceptor.AuthInterceptor"/>
        </mvc:interceptor>
        <!-- 可选认证拦截器（公开可浏览，登录用户可个性化） -->
        <mvc:interceptor>
            <mvc:mapping path="/api/learning-paths/**"/>
            <mvc:mapping path="/api/rankings/**"/>
            <bean class="com.aicode.interceptor.OptionalAuthInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

    <!-- ========== 统一异常处理 ========== -->
    <bean class="com.aicode.handler.GlobalExceptionHandler"/>
</beans>
