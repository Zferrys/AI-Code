<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicode.mapper.CodeReviewHistoryMapper">

    <resultMap id="BaseResultMap" type="com.aicode.entity.CodeReviewHistory">
        <id column="id" property="id"/>
        <result column="review_id" property="reviewId"/>
        <result column="version" property="version"/>
        <result column="code_content" property="codeContent"/>
        <result column="review_result" property="reviewResult"/>
        <result column="ai_response_time" property="aiResponseTime"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="BaseColumns">
        id, review_id, version, code_content, review_result, ai_response_time, create_time
    </sql>

    <select id="selectByReviewId" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM code_review_history
        WHERE review_id = #{reviewId}
        ORDER BY version ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO code_review_history (review_id, version, code_content, review_result, ai_response_time, create_time)
        VALUES (#{reviewId}, #{version}, #{codeContent}, #{reviewResult}, #{aiResponseTime}, #{createTime})
    </insert>
</mapper>
