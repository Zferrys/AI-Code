<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicode.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.aicode.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="avatar" property="avatar"/>
        <result column="role" property="role"/>
        <result column="status" property="status"/>
        <result column="last_login" property="lastLogin"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="BaseColumns">
        id, username, password, email, phone, avatar, `role`, `status`,
        last_login, create_time, update_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM `user` WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM `user` WHERE username = #{username}
    </select>

    <select id="selectByEmail" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM `user` WHERE email = #{email}
    </select>

    <select id="selectAllPaged" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM `user` ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM `user`
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (username, password, email, phone, avatar, `role`, `status`, create_time, update_time)
        VALUES (#{username}, #{password}, #{email}, #{phone}, #{avatar}, #{role}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <update id="updateById">
        UPDATE `user`
        <set>
            <if test="email != null">email = #{email},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="status != null">`status` = #{status},</if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateLastLogin">
        UPDATE `user` SET last_login = NOW() WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE `user` SET `status` = #{status}, update_time = NOW() WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE `user` SET password = #{password}, update_time = #{updateTime} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM `user` WHERE id = #{id}
    </delete>
</mapper>
