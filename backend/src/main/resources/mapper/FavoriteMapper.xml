<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicode.mapper.FavoriteMapper">
    <resultMap id="BaseResultMap" type="com.aicode.entity.Favorite">
        <id column="id" property="id"/><result column="user_id" property="userId"/>
        <result column="content_type" property="contentType"/><result column="content_id" property="contentId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>
    <sql id="BaseColumns">id, user_id, content_type, content_id, create_time</sql>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO favorite (user_id, content_type, content_id, create_time) VALUES (#{userId}, #{contentType}, #{contentId}, #{createTime})
    </insert>
    <delete id="deleteByUserAndTarget">DELETE FROM favorite WHERE user_id = #{userId} AND content_type = #{contentType} AND content_id = #{contentId}</delete>
    <select id="selectByUserAndTarget" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM favorite WHERE user_id = #{userId} AND content_type = #{contentType} AND content_id = #{contentId}
    </select>
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM favorite WHERE user_id = #{userId} ORDER BY create_time DESC LIMIT #{offset}, #{limit}
    </select>
    <select id="countByUserId" resultType="long">SELECT COUNT(*) FROM favorite WHERE user_id = #{userId}</select>
    <select id="countByTarget" resultType="long">SELECT COUNT(*) FROM favorite WHERE content_type = #{contentType} AND content_id = #{contentId}</select>
</mapper>
